import classNames from 'classnames/bind'
import styles from './ListUsers.module.scss'

const cx = classNames.bind(styles)

function ListUsers() {
    return <div>đây là trang list user</div>
}

export default ListUsers
